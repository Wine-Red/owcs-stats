我将通过改进全局布局、卡片样式和图表配置来提升数据可视化页面的美观度。

### 1. 全局布局与主题优化 (`Visualize.vue`)
- **现代化页头设计**: 重新设计页面标题区域，增加背景横幅或装饰元素，使其更具视觉冲击力。
- **筛选区域优化**: 改进“全局筛选”卡片的样式，使其与页面融合得更自然，操作更直观。
- **布局间距调整**: 优化页面边距和组件间距，增加留白，提升呼吸感。
- **卡片样式统一**: 统一所有图表卡片的阴影、圆角和悬停浮动效果，使其风格一致且更具质感。

### 2. 图表组件细节打磨
我将依次优化 `HeroBanChart.vue`、`MapPickChart.vue`、`TeamStatsChart.vue` 和 `PlayerStatsChart.vue`：
- **统一卡片标题**: 规范化所有图表的标题样式（字体大小、粗细、左侧装饰条），增强整体感。
- **图表视觉增强**:
    - **英雄禁用图表 (`HeroBanChart`)**: 为柱状图添加背景条 (`showBackground`)，优化渐变色，改进提示框 (`Tooltip`) 样式。
    - **地图选取图表 (`MapPickChart`)**: 优化地图背景图的滤镜效果，调整叠加图标的位置和大小，确保文字清晰可见。
    - **散点图表 (`TeamStatsChart` & `PlayerStatsChart`)**:
        - 使用更现代、对比度更高的自定义配色方案。
        - 优化网格线 (`splitLine`) 为虚线样式，减少视觉干扰。
        - 增强提示框 (`Tooltip`) 的格式化和样式，使其信息展示更清晰。
        - 优化坐标轴标签和刻度线的显示效果。

### 3. 视觉细节提升
- **排版优化**: 建立清晰的字体层级，优化标题和正文的对比度。
- **响应式适配**: 确保在移动端设备上，图表和布局依然保持美观和易读。
- **交互动画**: 为卡片加载添加淡入动画，提升用户体验的流畅感。

我将首先修改 `Visualize.vue` 进行全局布局调整，随后逐一优化各个图表组件。