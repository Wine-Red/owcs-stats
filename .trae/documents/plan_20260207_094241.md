I have analyzed the codebase and identified the necessary changes to allow data entry with placeholder player records and NULL `heroId`.

Here is the plan to implement these changes:

### 1. Backend: Update Database Model and Sync
*   **File:** `backend/models/PlayerStat.js`
    *   **Action:** Modify the `heroId` field definition to set `allowNull: true` (currently `false`).
*   **File:** `backend/database/index.js`
    *   **Action:** Update `sequelize.sync({ force: false })` to `sequelize.sync({ alter: true })`. This ensures the database schema is updated to reflect the `heroId` change without losing existing data.

### 2. Frontend: Update Data Entry Logic
*   **File:** `src/views/data-entry/DataEntry.vue`
    *   **Action:** Modify the `submitForm` method.
    *   **Details:**
        *   Relax the validation logic to allow submitting player stats even if `heroId` is missing, as long as a `playerId` is selected.
        *   Ensure `heroId` is sent as `null` in the payload when it is not selected.

### 3. Frontend: Update Data Management Logic
*   **File:** `src/views/data-manage/DataManage.vue`
    *   **Action:** Modify the `saveMapGameEdit` method.
    *   **Details:**
        *   Update the filter condition `ps.playerId && ps.heroId` to just `ps.playerId` to include players without a hero selected.
        *   Ensure `heroId` is converted to `null` if empty before sending the update request.

By implementing these changes, the system will support creating placeholder records for players without requiring full data entry initially, satisfying your requirements.