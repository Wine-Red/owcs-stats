I will implement the player personal data chart with three modes (Tank, Damage, Support) as requested.

### Backend Changes (`backend/controllers/StatsController.js`)
*   Modify `getPlayerStats` to return aggregated data instead of raw records.
*   The API will now group by `playerId` and sum up stats (kills, deaths, damage, etc.) and `totalDuration` (from `MapGame`).
*   Add support for filtering by `role` (Tank/Damage/Support) and `teamIds`.

### Frontend Changes (`src/views/visualize/Visualize.vue`)
*   **Role Selector**: Add a radio button group to switch between Tank, Damage, and Support modes (default: Damage).
*   **Player List Filter**: Update the player dropdown to only show players matching the selected role.
*   **Chart Implementation**:
    *   Update `updatePlayerStatsChart` to fetch the new aggregated data.
    *   Implement **Scatter Plot** (consistent with the "X axis..., Y axis..." requirement).
    *   **Tank Mode**: X-axis = Mitigation/10min, Y-axis = Deaths/10min (Inverted).
    *   **Damage Mode**: X-axis = Damage/10min, Y-axis = Eliminations/10min.
    *   **Support Mode**: X-axis = Healing/10min, Y-axis = Assists/10min.
    *   Data points will represent individual players.
