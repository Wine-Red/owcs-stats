I will modify the Dashboard page (`src/views/dashboard/Dashboard.vue`) to switch the focus from "Matches" to "Map Games" as requested.

### Key Changes:
1.  **Data Source**:
    *   The dashboard will now fetch and display `Map Games` (地图局) instead of `Matches` (比赛).
    *   I will update the `onMounted` hook to load map game data (`loadMapGames`).

2.  **Statistics Card**:
    *   Change the "Matches Count" (比赛数量) card to "Map Games Count" (地图局数量).
    *   It will display the total number of map games recorded.

3.  **Recent Activity List**:
    *   Replace the "Recent Matches" list with "Recent Map Games" (最近地图局).
    *   Each item will show:
        *   **Season & Map**: The season name and the map played (e.g., "Season 1 - King's Row").
        *   **Teams**: Team 1 vs Team 2.
        *   **Result**: The winning team will be highlighted.

4.  **UI Updates**:
    *   Update section titles and placeholder text to reflect "Map Games" instead of "Matches".

### Implementation Steps:
1.  Modify `src/views/dashboard/Dashboard.vue`.
2.  Update the script to fetch `mapGames` from the store.
3.  Refactor computed properties to use `mapGames` data.
4.  Update the HTML template to render map game information.
