I will split the `Visualize.vue` file into four independent chart components to improve code maintainability and structure.

### 1. Create Component Directory
- Create `src/views/visualize/components` folder.

### 2. Extract Components
I will create the following 4 Vue components in the new directory. Each component will encapsulate its own UI (including the Card wrapper), data fetching, and chart rendering logic. They will accept `seasonId` as a prop to react to global season changes.

#### A. `HeroBanChart.vue`
- **Content**: "Hero Ban Statistics" card.
- **Logic**: Fetch and display hero ban data.
- **Props**: `seasonId` (String).

#### B. `MapPickChart.vue`
- **Content**: "Map Pick Statistics" card.
- **Logic**: Fetch and display map pick data, including the custom map type icon overlay and image processing logic.
- **Props**: `seasonId` (String).

#### C. `TeamStatsChart.vue`
- **Content**: "Team Stats" card.
- **Logic**:
  - Fetch team statistics.
  - Manage local team filter (using Vuex to get team lists).
  - Render Scatter Plot (Damage vs K/D).
- **Props**: `seasonId` (String).

#### D. `PlayerStatsChart.vue`
- **Content**: "Player Personal Stats" card.
- **Logic**:
  - Fetch player statistics.
  - Manage local filters: Player Role (Tank/Damage/Support) and Player Select.
  - Implement the "Top 5" auto-selection logic.
  - Render Scatter Plot based on role.
- **Props**: `seasonId` (String).

### 3. Refactor `Visualize.vue`
- **Clean up**: Remove the extracted chart codes, state variables, and helper functions from the main file.
- **Integration**: Import and use the 4 new components, passing the `filterForm.seasonId` as a prop.
- **Layout**: Maintain the existing grid/layout structure to ensure the UI looks the same.
