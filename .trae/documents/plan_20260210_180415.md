I will implement the aspect ratio fix for team icons by pre-loading images and calculating their dimensions as requested.

### Implementation Plan

1. **Add State for Icon Sizes**

   * Create a reactive `teamLogoSizes` map to store the calculated `[width, height]` for each team's logo.

2. **Create Image Pre-loading Helper**

   * Implement `preloadTeamLogo(url)`: A helper function that loads an image and returns its natural dimensions.

   * Implement `calculateSymbolSize(width, height)`: Calculates the new width based on a fixed height of **26px** while maintaining the aspect ratio.

3. **Integrate Pre-loading Logic**

   * Modify `updateTeamComparisonChart` to await pre-loading of all team logos immediately after fetching `allTeamStats`.

   * This ensures that by the time `renderTeamChart` is called, the correct dimensions are available.

4. **Update Chart Configuration**

   * Modify `renderTeamChart` to retrieve the calculated size from `teamLogoSizes`.

   * Update the `symbolSize` property in the ECharts series configuration to use the `[width, height]` array format.

   * Remove `symbolKeepAspect: true` as we are now manually enforcing the aspect ratio via `symbolSize`.

### Code Changes

**src/views/visualize/components/TeamStatsChart.vue**

* Add `teamLogoSizes` ref.

* Add `preloadImage` function.

* Update `updateTeamComparisonChart` to call the preloader.

* Update `renderTeamChart` to use the dynamic `symbolSize`.

