I will implement the chart management feature by modifying `DataManage.vue` and `Visualize.vue`.

### 1. Modify `src/views/data-manage/DataManage.vue`

* **Import Icon**: Import `PieChart` from `@element-plus/icons-vue` for the new tab.

* **Add State**: Add a reactive `chartConfig` object to store the visibility state of 4 charts (`heroBan`, `mapPick`, `teamStats`, `playerStats`), defaulting to `true`.

* **Add Logic**:

  * `loadChartConfig`: Load configuration from `localStorage` key `visualize_chart_config` on mount.

  * `saveChartConfig`: Save the current configuration to `localStorage` and show a success message.

* **Update UI**:

  * Add a new `el-tab-pane` named "charts" (Label: "图表管理") in the main tabs.

  * Inside this tab, create a configuration interface divided into "Global Data Statistics" and "Detailed Data Statistics".

  * Use `el-switch` or checkboxes to toggle visibility for each chart.

### 2. Modify `src/views/visualize/Visualize.vue`

* **Add State**: Add a reactive `chartConfig` object similar to the one in Data Manage.

* **Add Logic**: In `onMounted`, read the `visualize_chart_config` from `localStorage` to initialize the state.

* **Update UI**: Apply `v-if` directives to the chart components (`HeroBanChart`, `MapPickChart`, `TeamStatsChart`, `PlayerStatsChart`) based on the `chartConfig` state.

  * *Note*: I will also add a simple check to hide the section containers if all charts within them are hidden, to avoid empty white space.

### 3. Verification

* Verify that changes in the Data Management page persist and correctly affect the Visualize page after a refresh or navigation.

