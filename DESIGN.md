# Design Reference for OWCS Stats

本文档基于当前 `src/views/visualize` 相关页面与组件的实际实现整理，目标是把现有可视化前端的视觉语言、布局规律和组件习惯沉淀为可复用的设计基线。它不是脱离代码的理想化规范，而是面向当前项目的“实现即规范”文档。

参考范围主要包括：
- `src/styles/visualize-theme.css`
- `src/views/visualize/Visualize.vue`
- `src/views/visualize/MatchDetail.vue`
- `src/views/visualize/UpcomingMatchDetail.vue`
- `src/views/visualize/TeamDetail.vue`
- `src/views/visualize/components/TournamentBanner.vue`
- `src/views/visualize/components/RegularSeasonBoard.vue`
- `src/views/visualize/components/RecentMatches.vue`
- `src/views/visualize/components/PlayerRadarChart.vue`

## 1. Visual Theme & Atmosphere

### 整体风格描述

当前可视化前端采用的是一种“轻底色、强对比、信息密集但不拥挤”的电竞数据界面风格：

- 基础背景以白色和极浅灰为主，弱化噪声，让图表、比分和队伍信息成为主视觉。
- 强调黑色与橙色的双主轴对比：黑色承载主信息与胜方权重，橙色承担强调、对位对象和高亮指标。
- 大多数详情页不使用传统“重卡片堆叠”，而是尽量做成无缝连续内容，减少视觉断层。
- 标题、比分、战队名大量使用偏科技感的数字/展示字体，强化电竞赛事氛围。
- 局部使用毛玻璃、轻阴影、渐变描边和背景图，但整体保持克制，不走厚重拟物路线。

### 设计哲学和原则

1. 数据优先
   页面首先服务于比分、战绩、图表和对位结论，装饰元素只能辅助信息，不应抢占视觉主导权。

2. 无缝沉浸
   详情页尽量去掉多余说明卡、冗余副标题和重复容器，让用户快速进入比赛分析本体。

3. 紧凑但不拥挤
   通过缩小 chip、高度、间距和次要字号，为图表和核心数据让出空间，而不是单纯删内容。

4. 对称对比
   凡是双队对位、双选手对位、左右阵营结构，尽量保持左右镜像和量级一致，降低理解成本。

5. 真实数据口径
   不能为了“好看”擅自推导错误指标。当前项目已明确：比赛详情分析中优先展示真实总量数据，预测页或赛季页才可使用 `/10m`。

6. 渲染稳定性
   图表切换时应优先保证量程稳定、布局稳定和可复现，避免因对比对象变化导致视觉尺度跳变。

## 2. Color Palette & Roles

### 主色调

- `#111111`
  用途：主文字、主按钮激活态、主队对比色、比分高亮、页签激活下划线、深色数据面。

- `#FF6A00`
  用途：强调色、次队对比色、关键高亮、选中态描边、橙色雷达面、胜方/强势指标点缀。

- `#FF9E0F`
  用途：品牌主橙、标题补色、品牌梯度起止色、加载态主题色、暖色强调补充。

- `linear-gradient(90deg, #FF6A00 0%, #FF9E0F 100%)`
  用途：品牌梯度、标题装饰、排名高亮、强调型视觉锚点。

### 中性色

- `#FFFFFF`
  用途：卡片背景、页面主体、浮层主体、激活 chip 背景。

- `#FAFAFA`
  用途：页面底色、模块分隔底、浅背景区块。

- `#F8F9FA`
  用途：浅灰容器、摘要 pill、表格头、二级卡片底。

- `#F4F4F5` / `#F0F2F5`
  用途：禁用或未激活 chip、对比分数胶囊、分段背景、轨道背景。

- `#EBEEF5` / `#E4E7ED`
  用途：标准边框、表格分隔线、弱描边。

- `#303133`
  用途：一级正文、正文深色文本。

- `#606266`
  用途：二级正文、图表轴标签、辅助信息。

- `#909399`
  用途：说明文案、占位文字、静态标签、非主胜方分数。

- `#DCDFE6` / `#C0C4CC`
  用途：分隔符、弱状态、装饰性符号。

### 语义化颜色

- `success: #28A745`
  用途：正向战绩、积分差正值、进行中或达标状态。

- `warning: #FF9E0F`
  用途：提示、品牌级强调、轻量告警。

- `error: #DC3545`
  用途：负向战绩、失败状态、差值负值。

- `live/error-soft: #F56C6C`
  用途：LIVE、红色提醒、即时性状态。

### 遗留颜色说明

当前 `UpcomingMatchDetail.vue` 中仍存在少量蓝色旧样式：

- `#409EFF`
- 蓝色加载 spinner
- 蓝色 hover/score winner

这些颜色属于历史残留，不应继续扩散。新的可视化设计应统一收敛到“黑 + 橙”为主轴，仅在确有语义需求时使用 success/error 色。

### CSS Variables 定义

建议将当前已存在和文档推荐的 token 统一收拢为以下变量：

```css
:root {
  --vis-primary: #ff9e0f;
  --vis-accent: #ff6a00;
  --vis-primary-strong: #111111;
  --vis-primary-gradient: linear-gradient(90deg, #ff6a00 0%, #ff9e0f 100%);

  --vis-bg-page: #fafafa;
  --vis-bg-subtle: #f8f9fa;
  --vis-bg-muted: #f4f4f5;
  --vis-bg-card: #ffffff;

  --vis-text-primary: #303133;
  --vis-text-strong: #111111;
  --vis-text-secondary: #606266;
  --vis-text-tertiary: #909399;
  --vis-text-disabled: #c0c4cc;

  --vis-border: #ebeef5;
  --vis-border-strong: #e4e7ed;

  --vis-success: #28a745;
  --vis-warning: #ff9e0f;
  --vis-error: #dc3545;
  --vis-live: #f56c6c;

  --vis-team-left: #111111;
  --vis-team-right: #ff6a00;
  --vis-team-left-soft: rgba(17, 17, 17, 0.08);
  --vis-team-right-soft: rgba(255, 106, 0, 0.12);
}
```

## 3. Typography Rules

### 字体家族

项目当前已经形成了明确的三层字体分工：

- 正文：`Inter`, `PingFang SC`, `Microsoft YaHei`, `Noto Sans CJK SC`, sans-serif
  用于正文、列表、表格、按钮、表单。

- 展示：`Oxanium`, `Inter`, `PingFang SC`, sans-serif
  用于比分、地图名、战队名、数字高亮、强视觉标题。

- 标题：`Orbitron`, `Inter`, `PingFang SC`, sans-serif
  用于少量章节标题、带电竞感的 section header。

- 中文安全兜底：`PingFang SC`, `Hiragino Sans GB`, `Microsoft YaHei`, `Noto Sans CJK SC`, `Source Han Sans SC`, `SimHei`, sans-serif

### 字号层级

建议把当前实现收敛为以下层级：

- `h1`: `38px / 800`
  用于赛季横幅主标题，如赛事名。

- `h2`: `28px / 800~900`
  用于页面主标题、站点抬头。

- `h3`: `24px / 900`
  用于战队详情主名、地图 Banner 主名。

- `h4`: `20px / 700~800`
  用于 section 主标题，例如积分榜标题。

- `h5`: `16px / 800~900`
  用于卡片标题、次级标题、页头中部标题。

- `h6`: `14px / 800~900`
  用于小组件标题、模块内小标题。

- `body-lg`: `15px / 500~600`
  用于普通 tab、次级正文。

- `body`: `13px / 500~700`
  用于列表、统计值、摘要信息。

- `small`: `12px / 600~700`
  用于胶囊、表头、辅助标签。

- `caption`: `10px~11px / 700`
  用于移动端 chip、图表轴名、超紧凑说明。

### 行高和字重

- 超大标题：`1.05 ~ 1.15`
- 一般标题：`1.2 ~ 1.3`
- 正文：`1.5`
- 紧凑数据行：`1.1 ~ 1.3`

字重使用建议：

- `900`: 战队名、比分、核心对位标题
- `800`: 卡片标题、tab 激活态、chip 激活态、关键数值
- `700`: 表头、标签、辅助标题
- `500~600`: 普通正文与辅助信息

### 数字排版

- 所有比分、K/D、战绩、地图分差、表格数值优先使用数值字体族。
- 数字默认右对齐或居中对齐，不与正文混排造成节奏断裂。
- 大数值使用千分位格式，避免视觉噪点。

## 4. Component Stylings

### 按钮样式规范

#### 1. 标签型按钮

用于主 tab、子 tab、切换导航：

- 默认态：透明背景，灰色文字，底部无强调。
- 激活态：深色文字 + 深色下划线。
- 悬浮态：仅改变文字颜色，不做大面积背景填充。
- 动效：`0.2s ~ 0.3s ease`，轻微即可。

#### 2. Chip / Pill 切换按钮

用于“比赛分析 / 选手总览”等模式切换：

- 外层容器：浅灰底、超大圆角。
- 默认态：透明背景 + 次级文字。
- 激活态：深色背景 + 白字。
- 高度应紧凑，移动端继续压缩。

#### 3. 返回按钮

- 采用 link 风格，不做实体按钮。
- 默认灰色，hover 变深。
- 图标与文字等高对齐。

#### 4. 行点击反馈

用于比赛卡片、交手记录、队伍名入口：

- hover 使用轻微上浮 `translateY(-1px ~ -2px)`。
- active 使用轻微缩放 `scale(0.98)`。
- 不应出现重弹跳或夸张阴影。

### 表单组件样式

#### 1. 顶部 Header 控件

适用于主页赛季切换、头图区域控制器：

- 允许使用毛玻璃半透明白底。
- 圆角偏大，推荐 `18px ~ 20px`。
- 激活选项白底浮起，文字转橙色。
- 适合放在半透明 header 或横幅标题区。

#### 2. 内容区紧凑表单

适用于图表卡片内部筛选：

- 白底实体输入框。
- 细描边替代重阴影。
- 统一圆角 `8px ~ 10px`。
- focus 态用橙色 inset 边框，而不是蓝色描边。

#### 3. Dropdown 菜单

- 浅色背景，圆角 `8px ~ 12px`。
- 选中项可使用深底白字。
- 长列表在桌面端允许多列布局，在移动端改为 1~2 列并占用 `90vw`。

### 卡片和容器样式

#### 1. 标准卡片

- 背景：白色
- 圆角：`12px ~ 16px`
- 边框：`1px solid #EBEEF5`
- 阴影：极轻，通常 `0 2px 8px rgba(0,0,0,0.02)` 或 `0 4px 12px rgba(0,0,0,0.04)`

#### 2. 分析卡片

- 结构上更强调内容区，不强调卡壳感。
- 标题与内容之间用 padding 建立层次，不依赖厚重头部条。
- 适合承载雷达图、职责对位、地图走势。

#### 3. 数据行容器

- 悬浮反馈轻微。
- 行高紧凑。
- 分隔线优先用浅灰实线或虚线。
- 高亮数值只高亮最关键列，不做整行高亮。

#### 4. 背景图卡片

如地图走势卡片、地图 Banner：

- 背景图必须可读，叠加深色渐变遮罩。
- 文字放在遮罩最深区域。
- 队标不加圆底，保持“悬浮”感。

## 5. Layout Principles

### 间距系统

当前项目的有效间距刻度可归纳为：

- `4px`: 极紧凑图标、内嵌 chip、微调级间距
- `6px`
- `8px`
- `10px`
- `12px`
- `16px`
- `20px`
- `24px`
- `32px`
- `40px`
- `48px`

建议：

- 组件内微结构优先使用 `4/6/8/10/12`
- 模块内 padding 以 `12/16/20/24` 为主
- 模块之间使用 `24/32/40`
- 大页面顶底留白使用 `32/48`

### 网格和断点

当前实现实际采用的是“12 栏主网格 + 组件内部局部 grid/flex”混合模式：

- 总览页：`12` 栏网格
- 数据流区：纵向 section 堆叠
- 对位卡：双栏 / 单栏切换
- 地图卡与比赛 tab：允许横向滚动

建议断点：

- `>= 1200px`: 桌面宽屏
- `769px ~ 1199px`: 平板 / 小桌面
- `<= 768px`: 移动端主断点
- `<= 420px`: 小屏手机微调断点，主要影响雷达图布局

### 对齐原则

1. 对位结构左右镜像
   左队靠右对齐，右队靠左对齐，中间留比分或 VS 区。

2. 文本层级上对齐
   标题、标签、数值尽量共基线，不出现跳跃式错层。

3. 数字列稳定
   数值列固定宽度，避免因为内容变化导致表格抖动。

4. 标题与控件分区明确
   章节标题靠左，筛选控件靠右；移动端再改成纵向。

5. 顶部切换与内容紧贴
   已有实现明确强调：切换按钮与下方分析区间距应尽量小，避免断层。

## 6. Depth & Elevation

### 阴影层级

当前项目采用低对比阴影体系，建议统一为：

- `Level 0`
  无阴影，仅白底 + 边框

- `Level 1`
  `0 1px 3px rgba(0, 0, 0, 0.04)`
  用于基础卡片、轻浮层

- `Level 2`
  `0 2px 8px rgba(0, 0, 0, 0.02)` 或 `0 4px 12px rgba(0, 0, 0, 0.04)`
  用于常规内容卡、比赛行容器

- `Level 3`
  `0 6px 14px rgba(17, 17, 17, 0.08)` 或对应橙色阴影
  用于激活 chip、选中态强调、局部 hover

- `Level 4`
  `0 8px 24px rgba(0, 0, 0, 0.08~0.12)`
  用于 dropdown、tooltip、强调浮层

### z-index 规范

建议统一为：

- `0`: 普通内容、背景图、遮罩下层
- `1`: 卡片内容、Banner 文本层
- `10`: loading panel、局部 overlay
- `50`: dropdown / popper / tooltip
- `100`: sticky header
- `1000+`: 仅保留给第三方弹层系统

原则：

- 能不用高 z-index 就不用。
- 局部图层应优先靠文档流和相对定位解决。
- sticky header 是全站少数需要固定高层级的元素。

## 7. Do's and Don'ts

### Do's

- 使用白底 + 轻边框 + 轻阴影，而不是厚重深卡片。
- 使用 `#111` 和 `#ff6a00` 作为主要对比色，保持全站一致。
- 优先压缩间距、字体和 chip 尺寸，而不是直接砍掉数据层级。
- 在对位类图表中固定同一职责的量程口径，确保切换时不抖动。
- 在详情页中尽量移除冗余副标题，让分析本体更沉浸。
- 对地图、比分、战队信息使用更强视觉层级，提升赛事感。
- 移动端优先保留横向滚动和 scroll-snap，而不是强行挤成多行。

### Don'ts

- 不要重新引入大面积蓝色主视觉，除非是明确的语义色。
- 不要在同一页面混用多套强调色逻辑。
- 不要使用厚阴影、厚描边、重玻璃效果，破坏轻量感。
- 不要让表格列宽随内容长度频繁变化，尤其是 K/A/D 和数值列。
- 不要为每个分析模块都写长说明文案，当前设计方向是更克制的无缝内容。
- 不要为了图表“漂亮”擅自改写数据口径。
- 不要在移动端让 chip、tab、队名、比分发生明显换行抖动。

## 8. Responsive Behavior

### 断点定义

- `Desktop Wide`: `>= 1200px`
- `Desktop / Tablet`: `769px ~ 1199px`
- `Mobile`: `<= 768px`
- `Small Mobile`: `<= 420px`

### 响应式适配规则

#### 1. 页面留白

- 桌面端内容区常用 `32px ~ 48px` 外边距。
- 移动端应压缩到 `8px ~ 12px` 级别。

#### 2. 头部

- Logo、标题、select 同行布局在移动端保留，但必须缩小字号和间距。
- 标题可改为上下双行以节省横向空间。

#### 3. 网格

- 双栏分析区在 `<= 1200px` 可降为单栏。
- 复杂比较卡在 `<= 768px` 基本都应切换为单列。

#### 4. 横向滚动

- 地图走势、长 tab、长 dropdown 允许横向滚动。
- 优先用 `overflow-x: auto` + `scroll-snap`，不要压缩到不可读。

#### 5. 图表

- 雷达图在移动端降低半径并适当下移中心点。
- 小屏 `<= 420px` 时应继续缩小 `radius` 并调整 `center`，避免顶部标签遮挡。

#### 6. Chip / Selector

- 移动端 chip 高度应控制在约 `28px` 左右。
- 左右对位的选手选择器仍保持横向对称，但 gap 进一步压缩。

#### 7. 表格与数据行

- 列宽继续固定，但整体字号减小。
- 优先压缩次要列，为选手名和核心数值让出空间。

## 9. Agent Prompt Guide

### 如何将本文档用于 AI 提示词

当让 AI 继续开发 `visualize` 相关页面时，提示词必须明确以下约束：

1. 说明这是 `OWCS Stats visualize` 体系内的页面，不是全新风格。
2. 明确采用“白底极简 + 黑橙主轴 + 轻阴影 + 高密度数据”的视觉体系。
3. 明确优先级：数据真实、图表稳定、紧凑布局、移动端可读。
4. 指出是否属于“赛季页”“比赛详情页”“预测页”“战队详情页”中的哪一类。
5. 如果涉及图表，明确是否应使用真实总量还是 `/10m` 指标。
6. 如果涉及移动端，明确要求保持横向滚动、chip 紧凑和图表不遮挡。

### 示例提示词模板

```text
请基于 DESIGN.md 为 OWCS Stats 的 visualize 模块设计/实现一个新组件。

页面类型：
- [填写：赛事总览 / 比赛详情 / 预测详情 / 战队详情 / 图表组件]

设计约束：
- 延续当前 visualize 风格，不要改成通用后台模板风。
- 整体使用白底、浅灰分隔、黑色主信息、橙色强调色。
- 保持高信息密度，但不要拥挤；优先压缩间距和次要元素。
- 卡片圆角控制在 12px~16px，阴影保持很轻。
- tab、chip、selector 使用紧凑风格，交互反馈轻微即可。
- 对位结构保持左右镜像，数字列尽量固定宽度。
- 如果是图表，对比刻度要稳定，避免切换对象后量程跳变。
- 如果是比赛详情分析，优先展示真实总量，不要擅自改成 /10m。
- 移动端需适配到 768px 以下，并考虑 420px 以下的小屏。

输出要求：
- 先给出组件结构和视觉思路。
- 再给出 Vue 代码与样式实现。
- 不要加入与当前体系不一致的蓝色主色、厚重阴影或冗长说明文案。
```

### 推荐补充提示

如果希望 AI 更稳定地产出符合项目风格的结果，可以追加：

```text
请优先复用当前项目 visualize 页面中已有的视觉语言：
- 黑橙对位色
- 轻边框白卡
- 紧凑胶囊按钮
- 轻量 hover 和 active 动效
- 移动端横向滚动与 scroll-snap
```

---

如果后续继续迭代 `visualize` 页面，建议同步维护本文件，优先更新：

- 新增或废弃的颜色 token
- 图表数据口径约束
- 新的断点规则
- 组件密度与移动端压缩策略
